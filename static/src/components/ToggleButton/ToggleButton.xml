<templates>
    <t t-name="ToggleButton">
        <div class="p-3 m-3 bg-warning rounded">
            <h2>ToggleButton</h2>
            <div>
                <button t-attf-class="{{ (state.active) ? 'btn-primary' : 'btn-secondary' }}"
                        class="btn" t-on-click="toggleState">Toggle</button>
            </div>
        </div>
    </t>
</templates>
